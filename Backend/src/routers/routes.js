const { Router } = require("express");

const router = Router();
const pool = require("../../db");

// router.get("/", (req, res) => {
//   res.send("using api route");
// });

module.exports = router;
